# K-Nearest Neighbors (KNN)

This notebook demonstrates the **K-Nearest Neighbors (KNN)** algorithm applied to handwritten digit classification using the `sklearn.datasets.load_digits` dataset.

## Contents
1. Importing libraries
2. Loading and preparing the dataset
3. Training a KNN model
4. Evaluating model performance (accuracy)
5. Visualizing predictions

## How to run
1. Install dependencies:
   ```bash
   pip install scikit-learn matplotlib numpy
